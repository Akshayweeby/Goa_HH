Drop these in before running /api/generate:

- template.png        <- Vikram's exported card background (blank photo/text slots)
- fonts/Inter-Regular.ttf
- fonts/Inter-Bold.ttf

Then update cards/config.py with the real slot coordinates.
